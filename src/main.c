#include <stdio.h>

/* Toolchain sanity check only — replace with real entry point once the
 * random projection tree/forest work begins (Phase 1+). */
int main(void) {
    printf("rpforest-ann: build OK\n");
    return 0;
}
